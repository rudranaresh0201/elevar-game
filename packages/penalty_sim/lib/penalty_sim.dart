/// A penalty shootout, as pure state: one goal, one keeper, five kicks each.
/// No Flutter, no Flame, `game_core`'s determinism contract throughout.
library;

export 'src/goal.dart';
export 'src/runner.dart';
export 'src/simulation.dart';
