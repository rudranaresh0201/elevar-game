/// Elevar's visual language.
///
/// The palette is not invented — it is sampled from the reference artwork, so
/// the built game matches the thing it was designed from rather than a
/// plausible-looking approximation of it.
library;

export 'src/colors.dart';
export 'src/theme.dart';
export 'src/chunky_button.dart';
export 'src/score_pill.dart';
